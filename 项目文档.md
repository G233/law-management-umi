# 湖南河清律师事务所管理系统 - 项目文档

## 📋 项目概述

**项目名称**: law-management-umi
**组织**: 湖南河清律师事务所
**版本**: 1.0.0
**技术架构**: Umi 3 + React 17 + TypeScript + Tencent CloudBase

这是一个面向律师事务所的综合管理系统，主要用于案件管理、审批流程、律师管理和法律顾问单位管理。系统采用云开发架构，使用腾讯云CloudBase提供后端服务和数据存储。

---

## 🏗️ 技术架构

### 前端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| **Umi** | 3.3.9 | React应用框架，提供路由、权限、构建等能力 |
| **React** | 17.x | UI框架 |
| **TypeScript** | 4.1.2 | 类型安全的JavaScript超集 |
| **Ant Design** | 4.16.2 | 企业级UI组件库 |
| **Ant Design Pro** | 6.x | 企业级中台解决方案 |
| **ahooks** | 2.10.0 | React Hooks库 |
| **moment** | 2.29.1 | 时间处理库 |

### 后端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| **Tencent CloudBase** | 2.x | 云开发平台（BaaS） |
| **@cloudbase/node-sdk** | 2.5.0 | Node.js云开发SDK |
| **wx-server-sdk** | 2.4.0 | 微信小程序服务端SDK |
| **esbuild** | 0.11.11 | 云函数打包工具 |

### 开发工具

| 工具 | 用途 |
|------|------|
| **pnpm** | 包管理器（推荐7.0.0+） |
| **whistle** | 本地代理调试工具 |
| **SwitchyOmega** | Chrome代理插件 |
| **prettier** | 代码格式化 |
| **lint-staged** | Git提交前代码检查 |

---

## 📁 项目结构

```
law-management-umi/
├── src/                          # 前端源代码
│   ├── pages/                    # 页面组件（约定式路由）
│   │   ├── CaseList/            # 律所案件信息列表页
│   │   ├── CaseApprove/         # 案件审批页面
│   │   │   ├── index.tsx        # 审批页面入口
│   │   │   ├── approvingCases.tsx  # 待审批案件
│   │   │   └── approvedCases.tsx   # 已审批案件
│   │   ├── addCaseApprove/      # 新建/修改案件报批
│   │   ├── CaseDetail/          # 案件详情页
│   │   ├── Advisory/            # 法律顾问单位信息
│   │   │   └── all.tsx          # 所有法律顾问单位
│   │   ├── UserManage/          # 律师信息管理
│   │   ├── My/                  # 个人中心
│   │   │   ├── index.tsx        # 个人中心入口
│   │   │   ├── caseList.tsx     # 我的案件
│   │   │   └── advisory.tsx     # 我的法律顾问单位
│   │   ├── User/                # 个人设置
│   │   │   └── index.tsx        # 个人信息设置
│   │   └── Login/               # 登录页面
│   │       └── index.tsx        # 微信扫码登录
│   │
│   ├── services/                # API服务层
│   │   ├── cases.ts             # 案件相关API
│   │   ├── user.ts              # 用户相关API
│   │   ├── advisory.ts          # 法律顾问相关API
│   │   ├── notice.ts            # 通知相关API
│   │   ├── until.ts             # 数据库操作工具函数
│   │   └── const.ts             # 常量和枚举定义
│   │
│   ├── components/              # 通用组件
│   │   ├── RightContent/        # 顶部右侧内容（用户头像、通知）
│   │   ├── NoticeIcon/          # 通知图标组件
│   │   ├── HeaderDropdown/      # 下拉菜单组件
│   │   └── Footer/              # 页脚组件
│   │
│   ├── models/                  # Umi数据模型（hooks）
│   │   └── useLawList.ts        # 律师列表管理hook
│   │
│   ├── hook/                    # 自定义hooks
│   │   └── useSafeState/        # 安全状态hook（防止组件卸载后更新）
│   │
│   ├── cloud_function/          # 云函数配置
│   │   └── index.ts             # CloudBase初始化
│   │
│   ├── app.tsx                  # 应用入口（运行时配置）
│   ├── global.less              # 全局样式
│   └── typings.d.ts             # TypeScript类型声明
│
├── functions/                    # 云函数代码（后端）
│   └── src/
│       ├── index.ts             # 云函数总入口（路由分发）
│       ├── get_case_list.ts     # 获取案件列表
│       ├── create_case.ts       # 创建案件
│       ├── change_case.ts       # 修改案件信息
│       ├── one_click_approve.ts # 一键审批
│       ├── fetch_all_advisory.ts# 获取所有法律顾问单位
│       ├── fetch_all_user.ts    # 获取所有用户
│       ├── update_user_info.ts  # 更新用户信息
│       ├── wx_login.ts          # 微信登录
│       └── until.ts             # 数据库操作工具
│
├── config/                       # 配置文件
│   └── config.ts                # Umi配置（路由、布局、构建）
│
├── cloudbaserc.dev.json         # CloudBase开发环境配置
├── cloudbaserc.prd.json         # CloudBase生产环境配置
├── package.json                 # 项目依赖
├── tsconfig.json                # TypeScript配置
└── .whistle.js                  # Whistle代理配置
```

---

## 🎯 核心功能

### 1. 案件管理

**功能概述**: 律师可以创建、查询、修改案件信息，管理律师可以审批案件。

**主要页面**:
- `/CaseList` - 所有审批通过的案件列表
- `/addCaseApprove` - 新建/修改案件报批
- `/CaseApprove` - 案件审批（仅管理律师）
- `/CaseDetail` - 案件详情查看
- `/My/caseList` - 我的案件

**案件数据模型**:
```typescript
interface Case {
  // 基本信息
  caseCause: string;              // 案由/涉案罪名
  litigant: string;               // 委托当事人姓名
  otherlitigant: string;          // 对方当事人姓名
  litigantPhone: string;          // 委托当事人联系方式
  litigantSituation?: string;     // 委托当事人基本情况
  otherLitigantSituation?: string;// 对方当事人基本情况
  clientSituation?: string;       // 委托人基本要求
  caseSituation?: string;         // 案件基本情况

  // 承办信息
  undertaker: string;             // 承办律师ID
  undertakerOpinion?: string;     // 承办人基本意见
  toll: string;                   // 拟收费金额及说明

  // 案件分类
  CaseType: CaseType;             // 民事/刑事/行政
  agencyStage: agencyStageType;   // 代理阶段

  // 审批信息
  approveStatus: CaseStatus;      // 审批状态
  approveMsg: string;             // 审批意见
  approver: string;               // 审批人ID
  approveTime: Date;              // 审批时间

  // 系统字段
  caseId: string;                 // 案号（自动生成）
  createTime: Date;               // 立案时间
  annex: string;                  // 附件
  _id?: string;
  unionId?: string;
}
```

**案件状态**:
```typescript
enum CaseStatus {
  AGREE = 0,      // 审批通过
  WAITING = 1,    // 等待审批中
  REJECT = 2,     // 审批未通过
}
```

**案件类型**:
```typescript
enum CaseType {
  Civil = 0,              // 民事案件
  Criminal = 1,           // 刑事案件
  Administrative = 2,     // 行政案件
}
```

**代理阶段**:
```typescript
enum agencyStageType {
  '一审',
  '二审',
  '再审',
  '执行',
  '侦查阶段',
  '审查起诉阶段',
  '审判阶段'
}
```

**案号生成规则**:
- 格式: `(年份)河清[案件类型简称]代[序号]号`
- 示例: `(2021)河清民代1号`, `(2021)河清刑代2号`
- 案件类型简称:
  - 民事: `民`
  - 刑事: `刑`
  - 行政: `行`

### 2. 案件审批流程

**审批流程**:
1. 律师创建案件 → 状态: `WAITING`
2. 通知管理律师审批
3. 管理律师审批:
   - 通过 → 状态: `AGREE`
   - 拒绝 → 状态: `REJECT`
4. 通知律师审批结果

**审批功能**:
- 单个审批: 在案件详情页进行
- 一键审批: 批量审批多个案件
- 审批历史: 查看已审批案件

**相关API**:
- `oneClickApprove(idList, fn, approverId, approveState, approveMsg)` - 批量审批

### 3. 用户管理

**用户角色**:
- **admin** - 管理律师
  - 审批案件
  - 管理律师信息
  - 删除律师（标记离职）
- **user** - 执业律师
  - 创建案件
  - 查看个人信息

**用户数据模型**:
```typescript
interface UserInfo {
  // 云开发用户信息
  avatarUrl: string;
  nickName: string;
  unionId: string;
  uid: string;

  // 自定义信息
  name?: string;            // 真实姓名
  phone?: string;           // 手机号
  role: string;             // 权限：'admin' 或 'user'
  licenseNumber?: string;   // 执业证号
  startDate?: Date;         // 执业起始时间
  sex?: string;             // 性别
  hasAddInfo: boolean;      // 是否完整填写了个人信息
  isLeave?: boolean;        // 是否离职
}
```

**主要页面**:
- `/UserManage` - 律师信息管理（管理员）
- `/setting` - 个人设置

### 4. 法律顾问管理

**功能概述**: 管理法律顾问单位信息，记录律师与顾问单位的聘任关系。

**数据模型**:
```typescript
interface AdvisoryType {
  name: string;             // 法律顾问单位名称
  timeRange: string;        // 聘任期限
  _id: string;
  unionId: string;          // 顾问律师ID
  createTime?: Date;        // 创建时间
}
```

**主要页面**:
- `/AllAdvisory` - 律所所有法律顾问单位
- `/My/advisory` - 我的法律顾问单位

### 5. 通知系统

**通知类型**:
```typescript
enum noticeType {
  approve = 'approve',              // 有人发起审批
  approveResult = 'approveResult',  // 审批结果通知
  msg = 'msg'                       // 其他消息
}
```

**通知状态**:
```typescript
enum noticeState {
  unReade = 0,   // 未读
  read = 1       // 已读
}
```

**通知触发时机**:
- 律师创建案件 → 通知管理律师审批
- 管理律师审批 → 通知承办律师审批结果

### 6. 身份认证

**登录方式**:
1. **微信扫码登录** - 主要登录方式
2. **邮箱密码登录** - 已废弃

**权限控制**:
- 未登录 → 重定向到登录页
- 未完善个人信息 → 强制跳转到设置页
- 已离职 → 无法访问系统

---

## 🔌 API接口设计

### 云函数架构

**调用方式**:
```typescript
// 统一通过 fn_call 云函数入口调用
cloudFunction(fnName: string, data: any)
  ↓
functions/src/index.ts (路由分发)
  ↓
对应的云函数文件
```

### 主要API列表

#### 案件相关

| API | 功能 | 参数 | 返回值 |
|-----|-----|------|--------|
| `get_case_list` | 获取案件列表 | `{current, pageSize, condition?, caseCause?, litigant?, undertaker?, caseId?, CaseType?}` | `{caseList: Case[], count: number}` |
| `create_case` | 新建案件 | `{Case对象}` | 创建的案件记录 |
| `change_case` | 修改案件信息 | `{case: Case, id: string}` | 更新结果 |
| `one_click_approve` | 一键审批 | `{idList: string[], approverId, approveState, approveMsg}` | 审批结果 |

**获取案件列表支持的筛选条件**:
- `condition`: 审批状态 (`AGREE` / `WAITING` / `REJECT`)
- `caseCause`: 案由
- `litigant`: 委托当事人
- `undertaker`: 承办律师
- `caseId`: 案号
- `CaseType`: 案件类型

#### 用户相关

| API | 功能 | 参数 | 返回值 |
|-----|-----|------|--------|
| `fetch_all_user` | 获取所有律师 | - | 用户数组 |
| `update_user_info` | 更新用户信息 | `{name, phone, role, licenseNumber, startDate, sex, isLeave, _id}` | `{updated: number}` |

#### 法律顾问相关

| API | 功能 | 参数 | 返回值 |
|-----|-----|------|--------|
| `fetch_all_advisory` | 获取法律顾问单位 | `{name?, undertaker?}` | `{data: Advisory[]}` |

### 前端服务层 (services/)

**案件服务** (`src/services/cases.ts`):
```typescript
// 查询
fetchCaseList(data: requestProp)          // 获取审批通过的案件
fetchApprovingCases(data: requestProp)    // 获取待审批案件
fetchMyCases(data: any)                   // 获取我的案件

// 创建和修改
createCase(value: Case, unionId: string)  // 新建案件
changeCase(value: Case, unionId: string, id: string)  // 修改案件

// 审批
oneClickApprove(idList, fn, approverId, approveState, approveMsg)

// 文件操作
uploadFile(data: any, unionId: string)    // 上传附件
downloadFile(file: any)                    // 下载附件
```

**用户服务** (`src/services/user.ts`):
```typescript
fetchUserInfo()                           // 获取当前用户信息
reSetUserInfo(data, unionId, refresh)     // 更新用户信息
updateUserInfo(_, row)                    // 管理员更新律师信息
deleteUser(_, row)                        // 删除律师（标记离职）
```

**法律顾问服务** (`src/services/advisory.ts`):
```typescript
featchMyAdvisory(data)                    // 获取我的法律顾问单位
featchAllAdvisory({name, undertaker})     // 获取所有法律顾问
newAdvisory(data, unionId)                // 新建法律顾问单位
```

---

## 💾 数据库设计

### CloudBase 数据库集合

| 集合名 | 描述 | 主要字段 |
|--------|-----|--------|
| **Cases** | 案件信息 | `caseId`, `caseCause`, `undertaker`, `approveStatus`, `approver`, `createTime` |
| **User** | 用户信息 | `name`, `phone`, `role`, `licenseNumber`, `unionId`, `isLeave` |
| **Advisory** | 法律顾问单位 | `name`, `timeRange`, `unionId`, `createTime` |
| **Notice** | 通知消息 | `title`, `msg`, `caseId`, `unionId`, `state`, `type` |
| **Cache** | 缓存数据 | `caseCauseList`, 年份和案件号统计 |

### 数据库索引建议

**Cases 集合**:
- `unionId` - 快速查询律师的案件
- `approveStatus` - 快速筛选审批状态
- `caseId` - 案号唯一索引
- `createTime` - 按时间排序

**Notice 集合**:
- `unionId` - 快速查询用户通知
- `state` - 筛选未读消息
- `createTime` - 按时间排序

---

## 🚀 开发指南

### 环境准备

1. **安装 Node.js**: 需要 Node.js 16
2. **安装 pnpm**:
   ```bash
   npm install -g pnpm
   ```
3. **安装 whistle** (可选，用于调试):
   ```bash
   npm install -g whistle
   ```
4. **安装 Chrome 插件 SwitchyOmega** (可选，配合whistle使用)

### 开发流程

1. **克隆仓库并安装依赖**:
   ```bash
   git clone <repository-url>
   cd law-management-umi
   pnpm install
   ```

2. **启动开发服务器**:
   ```bash
   pnpm start
   ```
   - 自动添加 whistle 代理配置
   - 启动 Umi 开发服务器
   - 默认访问地址: http://localhost:8000

3. **构建和部署**:
   ```bash
   pnpm build
   ```
   该命令会自动执行:
   - 云函数打包 (`fnBuild`)
   - 云函数部署到生产环境 (`fnDeployPrd`)
   - 前端构建

### 云函数开发

1. **本地开发云函数**:
   - 编辑 `functions/src/` 下的文件
   - 云函数使用 TypeScript 编写

2. **打包云函数**:
   ```bash
   pnpm run fnBuild
   ```
   - 使用 esbuild 打包
   - 输出到 `functions/fn_call/`

3. **部署云函数**:
   ```bash
   # 开发环境
   pnpm run fnDeployDev

   # 生产环境
   pnpm run fnDeployPrd
   ```

### 代码规范

**自动格式化**:
- 使用 Prettier 格式化代码
- Git 提交前自动执行 lint-staged

**手动格式化**:
```bash
pnpm run prettier
```

### 分支管理规范

- `main` - 主分支，用于部署到线上
- `feature/作者名_功能点_日期` - 功能分支
  - 示例: `feature/atomliu_doc_0504`

**贡献流程**:
1. 基于 `main` 分支创建功能分支
2. 开发并提交代码
3. `git push` 后会生成 PR 链接
4. 将 PR 链接发给项目负责人审核
5. 审核通过后合并到主分支

---

## 🔑 核心技术特性

### 1. Umi 约定式路由

**特点**:
- `pages/` 目录结构自动映射为路由
- 支持嵌套路由
- 支持权限控制

**示例**:
```typescript
// config/config.ts
routes: [
  {
    path: '/CaseApprove',
    component: '@/pages/CaseApprove/index',
    name: '案件审批',
    access: 'admin'  // 权限控制
  }
]
```

### 2. Pro Layout 布局

**配置** (`config/config.ts`):
```typescript
layout: {
  name: '湖南河清律师事务所',
  layout: 'side',  // 侧边栏布局
  locale: false
}
```

**自定义布局** (`src/app.tsx`):
```typescript
export const layout: RunTimeLayoutConfig = ({ initialState }) => {
  return {
    rightContentRender: () => <RightContent />,  // 右侧内容
    footerRender: () => <Footer />,              // 页脚
    menuHeaderRender: undefined,
    // ...其他配置
  };
};
```

### 3. CloudBase 云函数网关

**设计思路**:
- 所有后端API统一通过 `fn_call` 云函数入口
- 根据 `fnName` 参数路由到不同的处理函数
- 减少云函数数量，简化管理

**实现** (`functions/src/index.ts`):
```typescript
exports.main = async (event: any) => {
  const { fnName, data } = event;

  switch (fnName) {
    case 'get_case_list':
      return await get_case_list(data);
    case 'create_case':
      return await create_case(data);
    // ...其他路由
    default:
      return { error: 'Unknown function' };
  }
};
```

### 4. 自动案号生成

**规则**:
- 格式: `(年份)河清[类型]代[序号]号`
- 每年每种类型独立计数

**实现逻辑**:
1. 从 Cache 集合读取当前年份的案号计数
2. 根据案件类型递增对应计数器
3. 生成案号并更新计数器

### 5. useSafeState Hook

**问题**: React 组件卸载后调用 setState 会报警告

**解决方案**:
```typescript
// src/hook/useSafeState/index.ts
export default function useSafeState<T>(defaultValue?: T | (() => T)) {
  const unmountedRef = useRef(false);
  const [state, setState] = useState(defaultValue);

  useEffect(() => {
    return () => {
      unmountedRef.current = true;
    };
  }, []);

  const setSafeState = useCallback((value: any) => {
    if (!unmountedRef.current) {
      setState(value);
    }
  }, []);

  return [state, setSafeState] as const;
}
```

### 6. 聚合查询

**场景**: 查询案件列表时需要关联用户信息

**实现** (`functions/src/get_case_list.ts`):
```typescript
const $ = db.command.aggregate;

db.collection('Cases')
  .aggregate()
  .lookup({
    from: 'User',
    localField: 'undertaker',
    foreignField: 'unionId',
    as: 'undertakerInfo'
  })
  .match(/* 筛选条件 */)
  .sort({ createTime: -1 })
  .skip((current - 1) * pageSize)
  .limit(pageSize)
  .end();
```

---

## 🔐 权限管理

### 角色定义

| 角色 | 权限 |
|------|------|
| **admin** | - 审批案件<br>- 查看所有案件<br>- 管理律师信息<br>- 删除律师 |
| **user** | - 创建案件<br>- 查看自己的案件<br>- 修改个人信息 |

### 权限检查流程

**全局权限检查** (`src/app.tsx`):
```typescript
export async function getInitialState() {
  // 1. 检查登录状态
  const hasLogin = await checkIsLogin();
  if (!hasLogin) {
    return { hasLogin: false };
  }

  // 2. 获取用户信息
  const currentUser = await fetchUserInfo();

  // 3. 检查是否完善个人信息
  if (!currentUser?.hasAddInfo) {
    history.push('/setting');
  }

  // 4. 检查是否离职
  if (currentUser?.isLeave) {
    message.error('您已离职');
    return { hasLogin: false };
  }

  return {
    hasLogin,
    currentUser,
  };
}
```

**路由权限控制** (`config/config.ts`):
```typescript
{
  path: '/CaseApprove',
  access: 'admin',  // 只有 admin 可访问
  component: '@/pages/CaseApprove/index'
}
```

**组件内权限判断**:
```typescript
const { initialState } = useModel('@@initialState');
const isAdmin = initialState?.currentUser?.role === 'admin';

{isAdmin && <Button>审批</Button>}
```

---

## 📦 部署指南

### 前端部署

**构建命令**:
```bash
pnpm build
```

**部署方式**:
- 使用 GitHub Actions 自动部署到 GitHub Pages
- 将代码推送到 `main` 分支即可自动触发部署

### 云函数部署

**开发环境**:
```bash
pnpm run fnDeployDev
```

**生产环境**:
```bash
pnpm run fnDeployPrd
```

**环境配置**:
- 开发环境: `cloudbaserc.dev.json`
  - envId: `atom-2gbnzw0gde4242dc`
  - NODE_ENV: `development`
- 生产环境: `cloudbaserc.prd.json`

---

## 🐛 常见问题

### 1. 组件卸载后 setState 警告

**问题**: React 组件卸载后异步操作调用 setState

**解决方案**: 使用 `useSafeState` hook 代替 `useState`

```typescript
import useSafeState from '@/hook/useSafeState';

const [data, setData] = useSafeState([]);
```

### 2. 云函数冷启动

**问题**: 云函数首次调用响应慢

**优化方案**:
- 使用 esbuild 压缩打包
- 减少云函数依赖
- 保持云函数"热"状态

### 3. 案件列表加载慢

**问题**: 数据量大时列表加载慢

**优化方案**:
- 使用分页查询
- 添加数据库索引
- 使用聚合查询优化关联查询

---

## 📊 性能优化建议

### 前端优化

1. **按需加载组件**:
   ```typescript
   import { dynamic } from 'umi';
   const DetailPage = dynamic(() => import('./CaseDetail'));
   ```

2. **使用 useMemo 缓存计算**:
   ```typescript
   const filteredData = useMemo(
     () => data.filter(item => item.status === 'active'),
     [data]
   );
   ```

3. **ProTable 优化**:
   - 使用虚拟滚动
   - 减少表格列数
   - 使用分页

### 后端优化

1. **数据库索引**:
   - 在常用查询字段上添加索引
   - 使用复合索引优化多条件查询

2. **缓存机制**:
   - 案由列表缓存到 Cache 集合
   - 用户信息客户端缓存

3. **云函数优化**:
   - 使用 esbuild minify 压缩代码
   - 减少依赖包大小
   - 复用数据库连接

---

## 🔮 后续优化方向

### 功能优化

1. **案件搜索增强**:
   - 全文搜索
   - 高级筛选
   - 导出功能

2. **统计报表**:
   - 案件统计图表
   - 律师工作量统计
   - 收费统计

3. **移动端支持**:
   - 开发微信小程序
   - 响应式设计优化

4. **文件管理**:
   - 案件附件预览
   - 批量上传下载
   - 云端存储管理

### 技术优化

1. **升级到 Umi 4**:
   - 更好的性能
   - 更完善的类型支持

2. **引入状态管理**:
   - 使用 Redux/Zustand 管理全局状态

3. **单元测试**:
   - 添加组件测试
   - 云函数测试

4. **CI/CD 完善**:
   - 自动化测试
   - 代码质量检查
   - 自动部署流水线

---

## 📝 更新日志

### 最近提交

- `c9ac560` - feat: 新增删除律师的能力
- `cd72535` - feat: 修复无法更改法律顾问单位的问题
- `cc974c0` - 暂停新用户注册以提高系统安全性
- `d5dffcf` - 取消自动创建用户，提高安全
- `2ebc71f` - feat: 更新注释

---

## 📞 联系方式

**项目负责人**: [请填写]
**技术支持**: [请填写]
**问题反馈**: 提交 GitHub Issue

---

## 📄 许可证

本项目为湖南河清律师事务所内部使用系统，版权所有。

---

**文档生成时间**: 2025-11-05
**文档版本**: v1.0.0
